import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js "N" development indicator. Build/runtime errors are still shown.
  devIndicators: false,
};

export default nextConfig;
